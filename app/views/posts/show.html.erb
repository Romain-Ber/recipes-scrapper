<h1>show</h1>
<h2><%= @post.title %></h2>
<p><%= @post.content %></p>
<a href="<%= @post.url %>"><%= @post.url %></a>

<% @comments.each do |comment| %>
  <%= render "card_comment", comment: comment %>
<% end %>
